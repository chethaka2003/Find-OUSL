spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ulfs?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Edu@1076
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.example.com
    port: 587
    username: no-reply@ou.ac.lk
    password: your-smtp-pass
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: 8080

ulfs:
  auth:
    allowedDomains:
      - "ousl.lk"
    verificationLinkBase: http://localhost:8080/api/auth/verify
    tokenTtlMinutes: 60
    rateLimitPerIpPerHour: 20
    minPasswordLength: 8

